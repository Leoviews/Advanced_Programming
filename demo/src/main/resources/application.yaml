server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/banking_app
    username: root
    password: tejas@345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  data:
    mongodb:
      uri: mongodb://localhost:27017/banking_nosql

  main:
    web-application-type: servlet